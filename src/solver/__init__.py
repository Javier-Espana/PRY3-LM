# Paquete del motor de resolución (unificación, engine, indexación).
# Exportaciones previstas:
# - from .unify import unify, occurs_in, apply, Trail
# - from .engine import Engine
# - from .indexer import KnowledgeBase, Index
